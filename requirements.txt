# 核心依赖
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.46.0
accelerate>=0.26.0
deepspeed>=0.14.0
peft>=0.8.0
trl>=0.8.0

# 数据处理
datasets>=2.14.0
pandas>=1.5.0
numpy>=1.24.0
pillow>=9.0.0
pyarrow>=10.0.0
tqdm>=4.64.0

# 训练监控
tensorboard>=2.13.0
wandb>=0.15.0

# 工具库
scikit-learn>=1.3.0
matplotlib>=3.6.0
seaborn>=0.12.0

# GPU加速（已启用）
bitsandbytes>=0.41.0    # 8bit量化训练
# flash-attn>=2.3.0     # Flash Attention加速（需要单独安装）
# pip install flash-attn --no-build-isolation -v

# VLM特定依赖
qwen-vl-utils>=0.0.1    # Qwen-VL工具库 

# DINO子模块（依赖已统一到上述通用部分，保留原始需求便于查阅）
# torch>=2.1.0（覆盖原 dino>=1.13.0 的需求）
# torchvision>=0.16.0（覆盖原 dino>=0.14.0 的需求）
# pillow>=9.0.0
# pandas>=1.5.0
# tqdm>=4.64.0
# numpy>=1.24.0（覆盖原 dino>=1.21.0 的需求）
# scikit-learn>=1.3.0（覆盖原 dino>=1.1.0 的需求）