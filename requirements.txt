# 核心依赖
torch>=2.0.0
transformers>=4.45.0
accelerate>=0.25.0
deepspeed>=0.12.0
peft>=0.7.0
trl>=0.7.0

# 数据处理
datasets>=2.14.0
pandas>=1.5.0
numpy>=1.24.0
pillow>=9.0.0
pyarrow>=10.0.0
tqdm>=4.64.0

# 训练监控
tensorboard>=2.13.0
wandb>=0.15.0

# 工具库
scikit-learn>=1.3.0
matplotlib>=3.6.0
seaborn>=0.12.0

# 可选GPU加速
# bitsandbytes>=0.41.0  # 8bit量化训练（可选）
# flash-attn>=2.3.0     # Flash Attention加速（可选） 